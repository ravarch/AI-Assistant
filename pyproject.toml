[project]
name = "pij"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "playwright",
    "pydantic>=2.12.5",
    "webtypy>=0.1.7",
    "markupsafe",
    "jinja2",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
