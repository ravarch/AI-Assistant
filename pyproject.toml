[project]
name = "pij"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.3",
    "playwright>=1.57.0",
    "pydantic>=2.12.5",
    "webtypy>=0.1.7",
]

[dependency-groups]
dev = [
    "workers-py"
]
