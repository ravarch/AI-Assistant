# Async HTTP (Cloudflare explicitly supports async HTTP clients like httpx) 
httpx>=0.27,<0.29

# URL + domain parsing / normalization
idna>=3.6,<4
tldextract>=5.1,<6
publicsuffix2>=2.20191221,<3

# IP intelligence (pure python)
ipwhois>=1.2.0,<2

# Useful helpers (pure python)
python-dateutil>=2.9,<3
ujson>=5.9,<6

fastapi>=0.128.0
pydantic>=2.12.5
webtypy>=0.1.7
playwright>=1.57.0
uvicorn>=0.34.0
